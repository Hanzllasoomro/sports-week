import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Points Rules' };

export default function Page() {
  return (
    <main className="min-h-screen bg-background flex items-center justify-center">
      <p className="font-caps-label text-fog-text">Points Rules — coming in Phase 1</p>
    </main>
  );
}
